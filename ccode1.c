

#include <stdio.h>
// #include <stdio.h>
#include <stdlib.h>
// #include "string.h"

struct dis{
    int a;
    int b;
};

// typedef struct {
//     int valid;
//     int *data;
//     size_t size;
// } dis;

int main()
{

    // ios_base::sync_with_stdio(false);
    // cin.tie(NULL);
    // cout<<setprecision(7)<<fixed;

    struct dis *d1 = malloc(sizeof(struct dis));

    // d = malloc(sizeof(struct dis));

    //  MyObject *my1 = malloc(sizeof(MyObject));












    
    return 0;
}
